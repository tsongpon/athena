version: '3.8'

services:
  athena:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: athena-api
    ports:
      - "1323:1323"
    environment:
      # IMPORTANT: Change this in production!
      - JWT_SECRET=${JWT_SECRET:-your-secret-key-change-this-in-production}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:1323/ping"]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3

  # Example: Add PostgreSQL database (currently in-memory)
  # Uncomment when migrating to database persistence
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: athena-db
  #   environment:
  #     - POSTGRES_DB=athena
  #     - POSTGRES_USER=athena
  #     - POSTGRES_PASSWORD=${DB_PASSWORD:-changeme}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U athena"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

# volumes:
#   postgres_data:
